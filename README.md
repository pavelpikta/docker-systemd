# Docker systemd

![Docker Image CI](https://github.com/pavelpikta/docker-systemd/workflows/Docker%20Image%20CI/badge.svg?branch=master)

Docker images with systemd support to test Ansible roles

Docker images build and push to GitHub Packages

A Docker images based on `ubuntu`, `centos` that runs `systemd`
